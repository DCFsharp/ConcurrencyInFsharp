<Window x:Class="PhotoViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Downloader" Height="350" Width="400" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="{x:Type ListBox}">
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="2" CornerRadius="3" Margin="4">
                            <Image Source="{Binding Path=UriSource}"
                                   Stretch="Fill"
                                   Width="75" 
                                   Height="75"/>
                        </Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>

            <Setter  Property="ScrollViewer.HorizontalScrollBarVisibility"
                     Value="Disabled"  />
        </Style>
    </Window.Resources>


    <DockPanel>
        <Grid DockPanel.Dock="Top" Height="30">
            <Button Width="100" Margin="0,0,-220,0" Content="Download Sync" Click="Button_Click_Sync"></Button>
            <Button Width="100" Margin="0,0,220,0" Content="Download Async" Click="Button_Click_Async"></Button>
            <Button Width="100" Margin="0,0,000,0" Content="Download Agent" Click="Button_Click_Agent"></Button>
        </Grid>
        <ListBox x:Name="imageList" ItemsSource="{Binding}" DockPanel.Dock="Bottom" />
    </DockPanel>
</Window>
